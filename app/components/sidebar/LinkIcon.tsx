import { Icons } from "@/components/ui/icons";

export const LinkIcon = () => {
  return (
    <div className="w-10 h-10 rounded-md bg-slate-100 dark:bg-accent flex items-center justify-center">
      <Icons.externalLink strokeWidth={1.3} />
    </div>
  );
};
